# Minimal on purpose - the worker has no real HTTP server, just
# prometheus_client's own tiny built-in one for /metrics (Assignment 5).

psycopg2-binary==2.9.9      # PostgreSQL driver
boto3==1.34.131             # AWS SDK (SNS)
python-dotenv==1.0.1        # Load .env in development
prometheus_client==0.26.0   # /metrics endpoint (Assignment 5 - observability)
